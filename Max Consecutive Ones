class Solution {
    public int findMaxConsecutiveOnes(int[] A) {
      int ans=0;
      int len=0;
      for(int i=0;i<A.length;i++){
        if(A[i]==1){
        len++;
        ans=Math.max(ans,len);
      }else{
        
        len=0;
      }  
    }
    
    return ans;
}
}
